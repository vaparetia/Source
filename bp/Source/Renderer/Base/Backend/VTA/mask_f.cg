__nativecolor __regformat unsigned char4 main(__regformat unsigned char4 fragColor : FRAGCOLOR, float4 wPos : WPOS ) : COLOR
{
   float f = 2.0*frac(wPos.y/2.0);
   if (f > 1.0)
      return fragColor;
   else
      discard;
}
