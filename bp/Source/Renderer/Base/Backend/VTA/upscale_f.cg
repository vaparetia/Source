struct PixelInput_Generic
{
   half4 projPos        : POSITION;
   half2 texUV          : TEXCOORD0;
};

sampler2D sTexture         : register(s0);

float4 main( PixelInput_Generic input ) : COLOR
{
   return half4(h3tex2D(sTexture, input.texUV), 1.0);
}
