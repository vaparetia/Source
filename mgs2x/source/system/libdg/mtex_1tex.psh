; マルチテクスチャ用ピクセルシェーダー（テクスチャ１枚のみ）
; 
;
; 2002.04.21  K.Takabe
; 2002.08.21  E.Takaki変更

ps.1.1

tex t0							; ベーステクスチャ

mul_x2 r0, t0, v0				; 頂点カラーでベーステクスチャをモジュレート

;mul_x2 r0.a,   v0, t0			; アルファはテクスチャ０のみを参照
;mul_x2 r0.rgb, t0, v0			; 頂点カラーでベーステクスチャをモジュレート

