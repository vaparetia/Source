//
//	init.gcl
//	$Id: init.gcl.org,v 1.1 2002/02/01 06:08:31 usr01475 Exp $

#include	"equip.h"
#include	"varinit.h"
#include	"sload.h"
                                                                                            
#ifdef d:FIX_MODE
#define DEBUGFLAG 0
#else
#define DEBUGFLAG 1
#endif

chara MGSシステム初期化 init -debug d:DEBUGFLAG
command VRAM初期化

@スタート時初期設定
@タイトル用初期設定

eval( $w:コンフィグ設定 = $w:コンフィグ設定 | d:CONFIG_RADAR_OFF_INTRUDE );

#ifdef d:PAL
command ＰＳ２から言語設定を取り出す

#ifdef d:SPANISH
eval( $w:言語 = LANG_SPANISH );
command プロダクトコード設定 'SLES-50385'
#elseifdef d:ITALIAN
eval( $w:言語 = LANG_ITALY );
command プロダクトコード設定 'SLES-50384'
#else
command プロダクトコード設定 'SLES-50383'
#endif

#ifdef GERMAN_TRIAL
// 欧州体験版のドイツ語版の修正
eval( $w:言語 = LANG_GERMANY );
eval( $w:コンフィグ設定 = $w:コンフィグ設定 | d:CONFIG_BLOOD_OFF );
#endif

command 字幕言語設定

#endif


#ifdef d:DESIGN_PREVIEW

load 'preview' -r 'r_tnk0'

#else

#ifdef d:FIX_MODE
load  'n_title' -r 'r_title'
#else
load  'select' -r 'r_title'
#endif

#endif

// サウンドは常駐ステージで読み込むようにした
// chara サウンドマネージャー ＳＤマネ -p 0

